---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getArticles } from '../../lib/supabase';
import { stripMarkdown } from '../../lib/fallback-data';
import { getArticlePhotosForCategory } from '../../lib/player-photos';

// Fetch real gear articles from Supabase
let gearArticles: any[] = [];
try {
  gearArticles = await getArticles({ category: 'gear', limit: 30 });
} catch (e) {
  console.warn('Gear index: Supabase unavailable');
}

// Batch-fetch all article photos (player photos + local)
const articlePhotos = await getArticlePhotosForCategory(gearArticles, 'gear');

// Category grouping
function getCategory(slug: string): string {
  if (slug.includes('racket') || slug.includes('head-vs-wilson') || slug.includes('junior')) return 'Rackets';
  if (slug.includes('string') || slug.includes('gut') || slug.includes('tension')) return 'Strings';
  if (slug.includes('shoe') || slug.includes('apparel')) return 'Clothing & Shoes';
  if (slug.includes('ball-machine')) return 'Equipment';
  if (slug.includes('court') || slug.includes('surface')) return 'Courts';
  return 'Accessories';
}

function getCategoryColor(slug: string): string {
  const cat = getCategory(slug);
  if (cat === 'Rackets') return '#16a34a';
  if (cat === 'Strings') return '#2563eb';
  if (cat === 'Clothing & Shoes') return '#d97706';
  if (cat === 'Equipment') return '#dc2626';
  if (cat === 'Courts') return '#059669';
  return '#7c3aed';
}

// Icon mapping
const iconMap: Record<string, string> = {
  'best-tennis-rackets-2026': '🎾',
  'best-tennis-strings-guide': '🔧',
  'best-tennis-shoes-2026': '👟',
  'best-tennis-bags-2026': '🎒',
  'best-tennis-overgrips': '🤲',
  'head-vs-wilson-vs-babolat': '⚔️',
  'best-tennis-balls-2026': '🟡',
  'best-tennis-dampeners': '🔇',
  'tennis-racket-weight-guide': '⚖️',
  'best-tennis-sunglasses': '🕶️',
  'tennis-court-surfaces-equipment': '🏟️',
  'best-tennis-gadgets-tech': '📱',
  'best-junior-tennis-rackets': '👶',
  'natural-gut-vs-synthetic-strings': '🧵',
  'tennis-apparel-brands-2026': '👕',
};
---

<BaseLayout
  title="Tennis Gear — Rackets, Shoes & Equipment Reviews"
  description="Expert tennis gear reviews and guides. Find the best rackets, shoes, strings, and equipment for your game. Pro player setups and recommendations."
  ogImage="/og/gear.png"
>
  <div class="gear-page">
    <div class="container">
      <nav class="breadcrumbs"><a href="/">Home</a> <span class="sep">/</span> <span class="current">Gear</span></nav>
      <section class="page-hero">
        <h1>Tennis Gear</h1>
        <p class="subtitle">Expert reviews, pro player setups, and buying guides for every level{gearArticles.length > 0 && ` — ${gearArticles.length} guides`}</p>
      </section>

      {/* Hero Banner */}
      <div class="gear-hero">
        <img src="/images/gear/tennis-rackets.jpg" alt="Tennis rackets and equipment" loading="eager" onerror="this.parentElement.style.display='none'" />
        <div class="gear-hero-overlay">
          <h2>Find Your Perfect Setup</h2>
          <p>Rackets, strings, shoes, and accessories — reviewed by tennis experts</p>
        </div>
      </div>

      {gearArticles.length > 0 ? (
        <div class="articles-grid">
          {gearArticles.map((article, i) => (
            <a href={`/gear/${article.slug}/`} class={`gear-card ${i === 0 ? 'featured' : ''}`}>
              {articlePhotos.get(article.slug) ? (
                <div class="gear-card-img">
                  <img src={articlePhotos.get(article.slug)!} alt={article.title} loading="lazy" onerror="this.parentElement.classList.add('no-img')" />
                </div>
              ) : (
                <div class="gear-card-icon">{iconMap[article.slug] || '🎾'}</div>
              )}
              <div class="card-content">
                <span class="card-category" style={`color: ${getCategoryColor(article.slug)}`}>{getCategory(article.slug)}</span>
                <h2>{article.title}</h2>
                <p>{stripMarkdown(article.excerpt || article.meta_description || '')}</p>
                <span class="read-more">Read Guide →</span>
              </div>
            </a>
          ))}
        </div>
      ) : (
        <div class="empty-state">
          <p>Gear guides are coming soon. Check back shortly!</p>
        </div>
      )}

      <section class="related-section">
        <h2>Pro Player Equipment</h2>
        <p>See what rackets, strings, and shoes your favorite pros use</p>
        <div class="cta-links">
          <a href="/players/">Browse Players →</a>
          <a href="/rankings/">View Rankings →</a>
          <a href="/records/">Tennis Records →</a>
        </div>
      </section>
    </div>
  </div>
</BaseLayout>

<style>
  .gear-page {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 24px 60px;
  }

  .page-hero {
    padding: 48px 0 32px;
    border-bottom: 3px solid var(--court-green);
    margin-bottom: 32px;
  }
  .page-hero h1 {
    font-family: var(--font-heading);
    font-size: 2.8rem;
    text-transform: uppercase;
    color: var(--gray-900);
    margin: 0;
  }
  .subtitle {
    color: var(--gray-600);
    font-size: 1.1rem;
    margin: 8px 0 0;
  }

  /* Gear Hero Banner */
  .gear-hero {
    position: relative;
    border-radius: var(--radius);
    overflow: hidden;
    margin-bottom: 32px;
    height: 180px;
  }
  .gear-hero img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .gear-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(0,0,0,0.65) 0%, rgba(0,0,0,0.2) 100%);
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    padding: 32px;
  }
  .gear-hero-overlay h2 {
    color: white;
    font-family: var(--font-heading);
    font-size: 1.6rem;
    text-transform: uppercase;
    margin: 0 0 8px;
  }
  .gear-hero-overlay p {
    color: rgba(255,255,255,0.85);
    font-size: 0.95rem;
    margin: 0;
  }

  /* Articles Grid */
  .articles-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
    margin-bottom: 48px;
  }

  .gear-card {
    text-decoration: none;
    color: inherit;
    background: white;
    border: 1px solid var(--gray-200);
    border-radius: var(--radius);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: all 0.2s;
  }
  .gear-card:hover {
    border-color: var(--court-green);
    box-shadow: var(--shadow);
    transform: translateY(-2px);
  }

  /* Featured first card spans 2 columns */
  .gear-card.featured {
    grid-column: span 2;
  }
  .gear-card.featured .gear-card-img {
    aspect-ratio: 16 / 9;
  }
  .gear-card.featured h2 {
    font-size: 1.2rem;
  }

  .gear-card-img {
    aspect-ratio: 1 / 1;
    overflow: hidden;
  }
  .gear-card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 30%;
  }
  .gear-card-img.no-img {
    display: none;
  }

  .gear-card-icon {
    aspect-ratio: 1 / 1;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 3rem;
    background: var(--gray-50);
  }
  .gear-card.featured .gear-card-icon {
    aspect-ratio: 16 / 9;
    font-size: 4rem;
  }

  .card-content {
    padding: 16px 20px 20px;
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .card-category {
    font-family: var(--font-heading);
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    margin-bottom: 6px;
    display: block;
  }

  .card-content h2 {
    font-family: var(--font-body);
    font-size: 1rem;
    font-weight: 700;
    margin: 0 0 8px;
    line-height: 1.35;
  }
  .card-content p {
    color: var(--gray-600);
    font-size: 0.82rem;
    line-height: 1.6;
    margin: 0;
    flex: 1;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .read-more {
    color: var(--court-green);
    font-weight: 600;
    font-size: 0.85rem;
    margin-top: 14px;
  }

  .empty-state {
    text-align: center;
    padding: 60px 24px;
    color: var(--gray-500);
    background: var(--gray-50);
    border-radius: var(--radius);
    margin-bottom: 48px;
  }

  .related-section {
    text-align: center;
    padding: 40px;
    background: var(--gray-50);
    border-radius: var(--radius);
  }
  .related-section h2 {
    font-family: var(--font-heading);
    font-size: 1.4rem;
    text-transform: uppercase;
    margin: 0 0 8px;
  }
  .related-section p {
    color: var(--gray-600);
    margin: 0 0 20px;
  }
  .cta-links {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }
  .cta-links a {
    color: var(--court-green);
    font-weight: 600;
    font-size: 0.95rem;
  }
  .cta-links a:hover { text-decoration: underline; }

  @media (max-width: 1024px) {
    .articles-grid { grid-template-columns: repeat(2, 1fr); }
    .gear-card.featured { grid-column: span 2; }
  }
  @media (max-width: 768px) {
    .articles-grid { grid-template-columns: 1fr; }
    .gear-card.featured { grid-column: span 1; }
    .page-hero h1 { font-size: 2rem; }
  }
</style>
