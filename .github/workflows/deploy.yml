name: Build & Deploy

on:
  schedule:
    - cron: '30 4 * * *'
  repository_dispatch:
    types: [content-updated]
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'public/**'
      - 'functions/**'
      - 'astro.config.*'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - 'workers/**'
      - 'scripts/**'

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Cache Astro build
        uses: actions/cache@v4
        with:
          path: node_modules/.astro
          key: astro-build-${{ hashFiles('src/**', 'public/**', 'functions/**', 'astro.config.*') }}
          restore-keys: astro-build-

      - name: Install dependencies
        run: npm ci

      - name: Publish scheduled lifestyle article
        if: ${{ hashFiles('scripts/publish-lifestyle-osaka-met-gala-2026.mjs') != '' }}
        run: node scripts/publish-lifestyle-osaka-met-gala-2026.mjs
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}

      - name: Build Astro site (with freshness map + image sitemap)
        # 2026-05-17: switched from `npx astro build` to `npm run build` so the
        # prebuild step (validate-photos + generate-freshness-map) and the
        # postbuild step (generate-image-sitemap) actually run in CI. They
        # were running locally but silently skipped on the CI deploy.
        run: npm run build
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}

      - name: Build search index (Pagefind)
        run: npx pagefind --site dist

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist --project-name supertennis
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Notify search engines (IndexNow + sitemap ping)
        if: success()
        run: |
          INOW_KEY="91c5dc98807613af2ffb83361ebbd154"
          echo "Submitting top pages via IndexNow..."
          curl -s -X POST "https://api.indexnow.org/indexnow" \
            -H "Content-Type: application/json" \
            -d "{
              \"host\": \"super.tennis\",
              \"key\": \"${INOW_KEY}\",
              \"keyLocation\": \"https://super.tennis/${INOW_KEY}.txt\",
              \"urlList\": [
                \"https://super.tennis/\",
                \"https://super.tennis/rankings/\",
                \"https://super.tennis/players/\",
                \"https://super.tennis/news/\",
                \"https://super.tennis/calendar/\",
                \"https://super.tennis/sitemap-index.xml\"
              ]
            }" || true
          echo "Pinging Google sitemap..."
          curl -s "https://www.google.com/ping?sitemap=https://super.tennis/sitemap-index.xml" || true
          echo "Search engines notified"

      - name: Submit new pages to Google Indexing API
        if: success() && env.GOOGLE_SA_JSON != ''
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          GOOGLE_SA_JSON: ${{ secrets.GOOGLE_SA_JSON }}
        run: |
          node scripts/request-indexing.mjs --limit=100 --type=all || true

      - name: Telegram alert on failure
        if: failure()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -H "Content-Type: application/json" \
            -d "{
              \"chat_id\": \"${{ secrets.TELEGRAM_CHAT_ID }}\",
              \"text\": \"❌ *Build & Deploy FAILED*\\n\\nRun: [#${{ github.run_number }}](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})\\nBranch: ${{ github.ref_name }}\\nTrigger: ${{ github.event_name }}\",
              \"parse_mode\": \"Markdown\",
              \"disable_web_page_preview\": true
            }" || true
